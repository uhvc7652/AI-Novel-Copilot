# AI-Novel-Copilot

一个 **DSH（DeepSeek Harness）Web 插件**：把 DSH 变成中文长篇网文的写作台——左侧一个「小说」入口，右侧一个写作面板，工程以纯 Markdown 存在工作区里，AI 生成一律先进预览、由作者采纳后才落盘。

> 本文件是**入口**。需求的唯一真源是 `docs/00-requirements.md`，磁盘格式的唯一真源是 `docs/03-project-structure.md`。

---

## 现在的进度

| 阶段 | 内容 | 状态 |
|---|---|---|
| **P0** | 工程骨架、章节读写、数据格式、沙箱围栏、写作面板（卷章树 / 编辑器 / 保存 / 字数） | ✅ 完成，真实 GUI 验证 |
| **P1** | 生成闭环：续写 / 改写 / 扩写，显式输入装配，预览后采纳 | ✅ 完成，真实 GUI 验证 |
| **P2** | 设定库 CRUD（删除=存档）、卷章大纲与 `beats` 编辑、按章纲写整章、工程记忆与文件夹选择 | ✅ 代码完成、检查全绿；**写作子会话与按章纲写整章已在真实 GUI 确认**，其余待复核 |
| P3 | 前文检索问答（M5）+ 一致性检查（M6），先确定性后模型 | M5 ✅ 检索问答（确定性）· M6 ✅ 一致性检查（确定性 + 模型层）——代码完成、检查全绿；确定性两侧的 host 路由已在真实实例跑通，**模型层那一轮我的沙箱跑不通，需要你点一次**；面板待复核 · M5 的模型问答 ⬜ |
| P4 | 文风统一与润色 | ✅ 代码完成、检查全绿（**风格样本终于进了装配**、润色本章、去 AI 味检查 + 可定位的问题清单）；模型两轮与面板交互待复核 |
| P5 | 导出、快捷键、性能、错误处理 | ✅ **三块都完成**：百万字级性能（增量扫描缓存，真实实例实测 4–5×，见 `docs/10-p5-performance.md`）；导出 / 快捷键 / 错误处理（见 `docs/13-p5-polish.md`——导出已在真实实例 HTTP 跑通，面板交互待复核） |
| M7 | 修改记录（面板内行级 diff 与回滚） | ✅ 代码完成、检查全绿、真实实例 HTTP 全流程跑通（`docs/11-m7-history.md`）；面板交互待复核 |

改动记录见 `docs/00-requirements.md` 的「变更记录」；每一步的验证证据在对应的 `docs/0N-*.md` 里。

---

## 文档索引

| 文件 | 内容 |
|---|---|
| `docs/00-requirements.md` | 需求与决策（D1–D11）、模块 M0–M8、分期 P0–P5、技术架构 |
| `docs/01-spike-findings.md` | P0-0 技术验证：外部包如何同时做 host/client 插件、**安装方式陷阱** |
| `docs/02-p0-implementation.md` | P0 实施记录与验证证据；四条 DSH 约束 |
| `docs/03-project-structure.md` | **工程结构的唯一真源**：目录树、七类文件 frontmatter、身份与引用规则 |
| `docs/04-p1-status.md` | P1 实施记录；执行器推翻与重建、类型检查的教训 |
| `docs/05-p2-implementation.md` | P2 实施记录；三类任务输出、`/doc` 白名单、**删除=存档**的理由与 GUI 验证清单 |
| `docs/06-p3-m5-search.md` | P3/M5 实施记录；确定性检索（无模型、无索引文件）、答案怎么分层说依据、n-gram 的代价、GUI 验证清单 |
| `docs/07-p3-m6-checks.md` | P3/M6 实施记录；15 条一致性规则（第 15 条 `thread-quote` 是 P5 之后补的）、issue 的 key 为什么是事实、忽略项落盘偏差、故意不报的四种情况、GUI 验证清单 |
| `docs/08-p3-m6-model.md` | P3/M6 模型层；模型检查读什么/输出契约、解析为何在可核对性上更严、失败的一轮必须报失败（真 bug）、测试书 `.spike-novel/` |
| `docs/09-p4-m4-style.md` | P4/M4 实施记录；风格样本装配（补上一个真缺口）、润色与改写的边界、去 AI 味六类毛病、「定位到正文」怎么做 |
| `docs/10-p5-performance.md` | P5 性能：基准脚本怎么造百万字、**先量后改**、扫描缓存「缓存字节、不缓存判断」、为什么**不做**倒排索引、为什么**不做**目录令牌门控；**含一次被真实实例推翻的测量错误**（§1.3） |
| `docs/11-m7-history.md` | M7 修改记录：与 git 的边界、回滚为什么写 `after` 不写 `before`、时间戳为什么必须唯一、`Ctrl+Z` 为什么不绑在编辑器上、行级 diff 与折叠 |
| `docs/12-threads.md` | 伏笔记录：两个入口各自的理由（回收为什么只在正文做）、原句为什么存句子而不是偏移、`status` 与 `payoffIn` 为什么一起写、为什么把手写记录与面板合一 |
| `docs/13-p5-polish.md` | P5 收尾：导出（一个文件一本书、只导活章、txt 与 md 正文逐字相同、文件名带时间戳、章号前缀归一、预览只取开头）、快捷键的三条规则（带修饰键 / 完全匹配 / 公开）、错误处理的失败语气与「半失败」怎么说、重试为什么不能重跑旧闭包；**M6 第 15 条规则 `thread-quote`**（原句过期）与 `findQuote` 为什么必须 host/client 共用 |

---

## 目录

```
src/
├── index.ts              host 插件入口（路由声明 + inject）
├── novel/                host 半
│   ├── paths.ts          无依赖的路径/身份规则：章节、设定卡、大纲、文档白名单（浏览器也要用）
│   ├── words.ts          字数统计（中文字 + 英文词），无依赖
│   ├── document.ts       frontmatter 与纯 YAML 的读写（js-yaml）
│   ├── project.ts        工程布局、卷章树、卡摘要与反向索引、脚手架
│   ├── io.ts             ctx.fs + 沙箱策略 + 路径围栏 + 文档读写/目录/设定库/检索语料
│   ├── cache.ts          **P5 扫描缓存**：按新鲜度令牌复用解析结果（缓存字节，不缓存判断）
│   ├── history.ts        **M7 修改记录（纯模块）**：行级 diff、折叠、动作判定、条目解析
│   ├── search.ts         **M5 确定性检索**：实体与别名、n-gram 关键词、片段、五类答案（零依赖）
│   ├── checks.ts         **M6 确定性检查**：15 条一致性规则、稳定 issue key、双侧依据（零依赖）
│   ├── book-export.ts    **P5 导出（纯模块）**：md/txt 渲染、章号前缀归一、导出文件名（零依赖）
│   ├── quote.ts          **原句查找（纯模块，两边共用）**：findQuote 的标点/空白容忍、行号（M6 与面板定位同源）
│   ├── timeline.ts       **时间线表格（纯模块，两边共用）**：parseTimeline / renderTimeline（M6 的 timeline 两条规则与面板编辑器同源）
│   ├── cards.ts          **谁还在工作集里 + 哪种卡有哪些字段（纯模块，两边共用）**：存档的卡 + 放弃的伏笔 = 退休（规则/检索/任务/页签同源）；`CARD_FIELDS` 是「哪种卡有哪个字段」、`CARD_SECTIONS` 是脚手架分节（面板占位提示读同一份）
│   ├── writing.ts        **写作子会话**：作者会话下的一次性子代理（零 import，见下）
│   └── http.ts           /api/novel/* 路由与错误映射
└── client/               浏览器半
    ├── index.tsx         三处注册：tab 类型 / 面板 / 左侧页脚前门
    ├── Panel.tsx         外壳：工程树 + 章节编辑器 + 八个页签 + 快捷键 + 状态行 + 工程记忆
    ├── projects.ts       打开过的工程（localStorage）：去重、上限、容错解析
    ├── shortcuts.ts      **P5 快捷键（纯数据 + 纯匹配）**：绑定表、完全匹配、备忘单来源
    ├── SettingsView.tsx  设定库：卡列表（含「设定」通用卡）、按类型给字段的卡编辑、新建、存档/恢复、反向链接
    ├── TimelineEditor.tsx **时间线**：逐行编辑那张表（故事时间/事件/章节 id 可选、↑↓、加行、编辑原文）
    ├── OutlineView.tsx   大纲：全书主线、卷纲、章纲（beats 逐条编辑 + 出场角色/地点/引用的设定卡）
    ├── SearchView.tsx    检索：问题 → 答案（含依据）→ 可跳转的命中列表
    ├── ChecksView.tsx    检查：一致性报告（分组 / 依据 / 跳转 / 忽略）
    ├── ThreadsView.tsx   **伏笔记录**：未回收/已回收/已废弃三组 → 跳回埋点 / 跳到回收（选中原句）
    ├── HistoryView.tsx   **M7 修改记录**：版本列表 → 行级 diff → 点行定位 → 回滚（Ctrl+Z 在这里）
    ├── ExportView.tsx    **P5 导出**：格式 / 范围 / 预览 / 导出到 `exports/` / 下载 / 已导出列表
    ├── locate.ts         在正文里选中一句话 + 「作者指着哪一句」（M4/M7/伏笔共用，纯函数可测）
    ├── ListField.tsx     **逗号分隔的列表输入**：输入期间保留逗号，离开输入框才解析（别名/标签/身份/出场角色/地点共用）
    ├── IssueList.tsx     模型检查/去 AI 味检查的结论列表（句子 + 依据 + 建议 + 定位到正文）
    ├── issues.ts         模型结论的 JSON 宽容解析（无 quote 也无 basis 的丢弃）
    ├── TaskBar.tsx       任务条：装配 → 流式 → 预览 → 采纳；拆章确认与建章；检查报告
    ├── tasks.ts          任务定义与显式输入装配（prose / doc / plan / issues 四类输出、
    │                     六个章节任务、文风规则与样本）
    ├── plan.ts           拆章 JSON 的宽容解析
    ├── ui.ts             共享样式、状态标签、PanelEnv
    ├── runner.ts         轮询 host 的生成结果
    └── api.ts            路由封装 + 失败的码（`errorCodeOf`：`novel/*` 与 `FS_*` 才值得打给作者）
```

`spike/` 放可复跑的检查脚本（见下），不是产品代码。`tools/` 放**你自己在终端里跑**的运维脚本（见下）——面板的能力面做不到的事（删文件、改文件名），脚本可以，因为那是你在动自己的文件。

---

## 开发

```sh
pnpm install
pnpm run check          # typecheck + build + format-check + client-load-check，改完跑这个
pnpm run typecheck      # 只跑 tsc --noEmit
pnpm run build          # 产出 lib/index.js（host）与 lib/client.js（浏览器）
pnpm run watch          # 开发期增量构建
```

安装进 DSH profile —— **必须用 `link:`，不要用 `file:`**：

```sh
node <dsh>/apps/cli/lib/bin.js plugin --profile <name> add link:E:/GameProject/AI-Novel-Copilot
```

> ⚠️ `file:` 在**跨卷**时退化成拷贝（本机：源码在 E:，profile 在 C:），profile 会冻结在安装那一刻，之后所有重建都到不了 GUI，而界面上毫无提示。详见 `docs/01-spike-findings.md`。
>
> ⚠️ 用 `link:` 之后仍然有一半要换进程：**浏览器半刷新页面即生效，host 半（新路由、新行为）必须起一个新进程**。DSH 的 `patchReload: live` 只热重载 `cordis.patch.yml`，不替换插件的 JS 模块（源码证据与判定方法见 `docs/01-spike-findings.md` §4）。面板里新加的路由报 404 时，先看 `/api/novel/ping` 返回的 `routes` —— 那就是运行中的 host 半真正注册了哪些路由。

启动（开发用隔离实例，不动日常 profile）：

```sh
$env:DSH_HOME='E:\GameProject\AI-Novel-Copilot\.dsh-home'
$env:DSH_NOVEL_ROOT='E:\GameProject\AI-Novel-Copilot\.spike-novel'
$env:DSH_PERMISSION_MODE='danger-full-access'
node <dsh>/apps/cli/lib/bin.js --profile novel --no-open --port 3099
```

> `pnpm dsh`（= `node --import tsx/esm`）在受限沙箱下会因 esbuild 子进程 `spawn EPERM` 失败，改用已构建的 `apps/cli/lib/bin.js`。

---

## 检查脚本（`spike/`）

| 脚本 | 验什么 |
|---|---|
| `format-check.mjs` | **直接 import 真 `.ts` 模块**（`node --experimental-transform-types`，346 项）：slug/卡路径/文档白名单、章纲与反向索引、设定卡摘要、frontmatter 往返、拆章 JSON 解析、**检索**（疑问词剥离/别名展开/n-gram 命中与片段坐标/五类答案/矛盾上报/**答案落在存档章上时明说**）、**一致性检查**（15 条规则各一次触发 + 故意保持安静的那些 + key 稳定性 + 忽略划分 + **退休的卡（存档/放弃）不参与命名与生命周期、存档章不参与「关于它自己」的规则，而两者的 id 仍然有效**）、**模型检查**（结论解析的宽容与严格、任务装配把规则已报的问题排除、没有开章时拒绝）、**失败的一轮不装成成功**（stub 传输层驱动 `startRun`）、**任务装配**（stub `fetch` 断言 prompt 与输入清单）、**host IO 层**（内存 `ctx.fs` 替身：脚手架、白名单、wordCount 盖章、建卡与建章规则、设定库、目录列举、章节存档、检索语料、检查报告与忽略项落盘）、**扫描缓存**（复用/面板外改动/面板外删除/反向链接跟着章节走/**没有版本号时不许缓存**/两个工程不串，替身可在三种后端间切换）、**修改记录**（行级 diff 的七种形状 + 折叠不丢行、路径与时间戳规则、写入即记录/回滚即一次写入/回滚可再回滚/一字未改不产生版本/同名 id 不串、**记录写不进去时的警告**）、**伏笔**（id 自动编号、章节 id 从路径推出、原句与生命周期读回、缺字段保持缺失、生命周期章节排序去重、io 往返到面板摘要、非伏笔卡不带空壳、**页签/检索/任务三处共用同一个「退休」判断**）、**定位取词**（逐字命中、标点与换行差异、找不到时不返回假位置）与**「作者指着哪一句」**（选区/光标所在行/空行/没有编辑器）、**写作子会话**（假 host 跑通 `meta.origin`/描述符/`followup`/流式结算）、**工程记忆**（去重/上限/容错解析）、**导出**（md/txt 的分层与逐字一致、存档章不出门、卷/单章范围、四种拒绝、章号前缀归一与对不上时不改、文件名规则、**预览只取开头而统计仍是整份**、写进 `exports/` 且不留修改记录）、**快捷键**（完全匹配、多一个修饰键不匹配、没有无修饰键的可打印键、与页签数组逐位对应）、**错误文案**（stub `fetch` 喂 404 HTML；抛异常时的「没能连上 DSH host」）、**原句查找与 `thread-quote`**（host 与面板拿到的是同一个 `findQuote`；原句还在/被改写/只差标点/回收章里任一命中/只有空白 五态）、**退休的文件**（存档与放弃的卡、存档章；`isRetiredCard` / `liveThreads` / `resolveEntities` / 拆章任务的可用设定清单，以及**存档章的 id 与章号照旧参与**这条例外）、**时间线表格**（表头/分隔行/多章一格/没有 id 的行照样读出来、只换表格而表外文字一字不动、叙事序按行号重排、无表时追加、往返一致、单元格里的竖线换全角、↑↓ 的边界）、**卡片字段进 prompt**（`age`/`gender` 读得出来、没填的保持缺失、`cardFacts` 只列填了的、空卡什么都不说，以及整章任务真的带上那一行）、**运维脚本的纯规则**（什么算已存档、后缀加在哪、怎么还原、以 `.md` 结尾的后缀被拒绝、`--apply` 才动盘、非终端里「确认」永远是「否」） |
| `client-load-check.mjs` | 桩掉模块加载器与 ctx，执行构建产物，渲染两个注册组件 + **每个面板表面各以真实数据渲染一遍（现在 21 次渲染）**：检索页、检查报告、修改记录的两态（有文档 / 没打开文档）、伏笔页的两态（有伏笔 / 还没有伏笔）、导出页的两态（默认 / 打开了一章且未保存）、**时间线编辑器的两态（有表 / 还没有表）**、模型结论列表的四种状态（后者喂真结论以执行「有结论」分支），并检查关键文案；**强制 `inject` 必须是工厂函数**；**源码规则：变暗的文字样式（`metaLine`）不得包住按钮/输入框** |
| `substance-check.mjs` | 空模板判定（空的世界观/卷纲/文风不该进 prompt） |
| `yaml-merge-check.cjs` | 改 `novel.yaml` 一个字段，其余字段与类型全部保留 |
| `host-api-check.mjs` | 对着真实实例跑完整 HTTP 流程（含 `/doc` `/dir` `/cards` `/search` `/checks` `/export`、存档、拆章建章、白名单拒绝、忽略与保存报告） |
| `perf-check.mjs` | **P5 性能基准**：合成百万字工程（固定种子）+ 照抄 `dsh-fs-local` 系统调用的真 `ctx.fs`（`realpath` 令牌、列举里逐子项 `realpath`+`stat`、读前 `stat`），按方法拆账量冷/暖的 `snapshot`/`search`/`check`；每次新建 IO 即等价于改动前的全量重扫，所以对照在同一次运行里量出来。`--chapters N` 改规模 |
| `fs-cost-check.mjs` | **`ctx.fs` 到底贵在哪**：拆开一次目录列举（`readdir` / `+stat` / `+realpath` / fs-local 的完整做法），并用 20 轮速率测「目录令牌能不能当子项集合变化的信号」。P5 的两个设计决定都是被它的输出否掉的 |
| `writing-agent-check.mjs` | 写作子会话的端到端烟雾测试（走 `/api/novel/task`） |

---

## 运维脚本（`tools/`，作者自己在终端里跑）

面板的能力面里没有删除、也没有重命名/移动（`ctx.fs` 只有 resolve/stat/lstat/list/read/write/edit），所以「把已存档的文件清掉」「把已存档的文件改名藏起来」这两件事做成脚本——**它们跑在你的终端里，用的是你的权限**，与插件不越沙箱那条原则不冲突：那是你在动自己的文件。

**双击就能用**（不想开终端的话）：`tools/archive-delete.cmd` 与 `tools/archive-suffix.cmd`——双击，或者在资源管理器里**把小说工程目录拖到它上面**。它们会：切到 UTF-8 代码页 → 跑脚本 → 让你在脚本里输路径（拖了就免输）→ 先给你看预演 → 问一句「现在真的做吗？」→ 做完 `pause` 等你按键。**不是终端（管道/重定向）时永远停在预演**，所以同一个文件放进脚本里跑也不会自己动手。

```sh
# 终端里的用法（默认预演，不加 --apply 不动磁盘）
node --experimental-transform-types tools/archive-delete.mjs   <小说工程目录>
node --experimental-transform-types tools/archive-suffix.mjs   <小说工程目录>
node --experimental-transform-types tools/archive-delete.mjs   <小说工程目录> --apply
node --experimental-transform-types tools/archive-suffix.mjs   <小说工程目录> --apply
node --experimental-transform-types tools/archive-suffix.mjs   <小说工程目录> --restore --apply

# 或者用 package.json 里的别名（参数直接跟在后面，不要写 --）
pnpm run archived <小说工程目录> --apply
pnpm run archived:suffix <小说工程目录> --apply
```

> **`.mjs` 不能双击运行**：Windows 上没有关联时双击只会让你「选择打开方式」，就算关联到 `node.exe`，双击时**没有参数**、黑窗口**一闪就关**，而这两个脚本必须要一个工程目录。所以双击入口是 `.cmd` 包装（上面那两个），交互提示全部放在 Node 脚本里。

| 脚本 | 做什么 | 关键设计 |
|---|---|---|
| `archive-delete.mjs` | 删掉工程里所有 `archived: true` 的文件 | **预演是默认**（要 `--apply` 才动盘）；动手前逐条列出**删了会有什么后果**——哪些文件的 frontmatter 还提到这个 id（那些引用会变成 `missing-ref` / `thread-ref` / `timeline-ref`）、这一章的章号后面还有没有章（有的话删掉会报 `chapter-gap`）；只碰 `chapters/ settings/ outline/ style/` 这四棵树，不动 `.novel/` 与 `exports/` |
| `archive-suffix.mjs` | 给已存档的文件加后缀（默认 `.archived`），或 `--restore` 去掉 | 后缀加在**扩展名之后**（`c0009.md` → `c0009.md.archived`）：名字一旦不以 `.md` 结尾，插件就完全不读它（章节按扩展名找、卡片按 `settings/<类型>/<id>.md` 找）；`c0009.archived.md` 那种写法会被当成新章节扫描到并立刻报 `id-mismatch`，所以脚本直接拒绝以 `.md` 结尾的后缀 |
| `archive-delete.cmd` / `archive-suffix.cmd` | 上面两个脚本的**双击入口**：双击、或把工程目录拖上去 | 只做三件事：`chcp 65001`（中文输出）→ `node <脚本>` → `pause`；**所有交互提示都在 Node 脚本里**——因为 `chcp` 之后 cmd 的 `set /p` 读重定向输入会失效，把提示写在批处理里正好是错的（这条是实测出来的，见「环境事实」第 4 条） |

两个脚本都只认 `archived: true`（YAML 布尔，用插件自己的 frontmatter 解析器读，不是第二套正则），都会拒绝看起来不像小说工程的目录（路径给错了代价很大），改名遇到同名文件时跳过而不是覆盖。删掉的内容仍然躺在 `.novel/history/<文档路径>/` 里（面板保存过的每一版全文）以及 git 里。

---

## 七条设计上的教训（都写进了文档）

1. **执行器绝不能是「当前会话」。** 第一版把提示词发进作者与编码 agent 的同一个会话，结果"小说续写"由编码 agent 回答——留痕里躺着的是聊天记录而不是正文。现在每次任务在 host 侧新建一个独立的一次性子代理：空历史、`systemPrompt.section({ complete: true })` 让写作指令成为**完整**系统提示词、`tools.restrict({ allow: [] })` 禁掉工具（怎么让它不出现在侧栏见第 4 条）。
2. **有类型检查却不跑，等于没有。** 一个「按钮恒灰」的低级错误漏到了作者手里。运行期渲染检查抓不到它——组件渲染通过，但那段 JSX 一次都没被执行。
3. **被能力面挡住的请求，要么降级要么明说，不要绕过围栏。** `ctx.fs` 没有删除原语（resolve/stat/lstat/list/read/write/edit，仅此而已），所以「删卡」和「删章」都实现为 `archived: true` 存档——章节还额外保留 id 与章号，恢复时不撞引用——而不是拿 `node:fs` 去 unlink：沙箱策略是作者对自己文件说的话。同理，检查脚本要么跑真模块（`--experimental-transform-types` 直接 import `.ts`），要么在注释里写清它抄了什么、为什么可以抄。
4. **没验证的假设不要写成「所以」。** P1 记下「不设 `cwd` → 不进作者侧栏」，后果是每次任务都在侧栏留下一条打不开的会话（`session/not-found`）。DSH 里隐藏会话的开关只有一个：header 的 `origin: 'subagent'`。现在写作子会话按 DSH 自己的一次性子代理创建（`origin` + `parentSession` + `subagent/descriptor`），提示词走子会话自己的 `followup`——因为 Session API 正是用 `agent-busy` 拒绝按普通 id 驱动子代理的。
5. **断言证明你想到的事，读一遍真实输出才能发现你没想到的事。** M5 的检索检查全绿之后，我把引擎输出原样打出来读了一遍，立刻发现「是哪章」被当成了检索词、剥剩一个「过」字导致**每一章都算命中**——两条都被断言漏掉了（`06` §3.2）。同一个教训在 P5 的导出上又应验一次：断言里的章标题恰好不带章号，真实实例导出的却是 `第 2 章 第二章 巡夜人`——中文网文的标题本来就常自带章号，只有把真实输出读一遍才看得见（`13` §1.1）。检查脚本是防止回归的，不是发现自己没想到的东西的。
6. **降级路径也是一条路径，不写测试就等于没有。** P5 的扫描缓存有三条降级：列举带令牌 / 不带令牌退 `stat` / 哪里都没有令牌就不缓存。第三条我写成了 `continue`——不是「不缓存」，是**根本不读这个文件**，整个工程在那种后端上会静默变成空书。`format-check` 里那条「宁可每次重读」的断言当场变红（`10` §4.1）。降级分支比主分支更容易写错，因为它平时不跑。
7. **尺子也要先量一遍。** P5 的基准替身漏了 `dsh-fs-local` 列举时对**每个子项**做的 `realpath`（Windows 上一次约等于八次 `stat`），把暖扫描量快了 9 倍，据此写下的「10–14×」是错的；真实实例一跑就露馅（`10` §1.3）。替身也是代码——它错的时候不报错，只是安静地把最重要的成本算成零。同一轮里「拿目录令牌跳过列举」也被自己的探针否掉：单次采样两次给出相反答案，20 轮速率才看清它只是个**延迟落地**的信号（`10` §3.4）。

---

## 交接状态（2026-09-12，下一段对话从这里接）

### 代码到哪了

| 期 | 状态 |
|---|---|
| P0 / P1 | ✅ 完成，真实 GUI 验证过 |
| P2 | ✅ 代码完成、检查全绿；**写作子会话与按章纲写整章已在真实 GUI 确认**，其余待复核 |
| P3 | ✅ 代码完成、检查全绿：M5 检索问答（确定性）· M6 一致性检查（确定性 15 条规则 + 模型层） |
| P4 | ✅ 代码完成、检查全绿：文风规则**与样本**装配、润色本章、去 AI 味检查 + 可定位的问题清单 |
| P5 | ✅ **三块都完成、检查全绿**：百万字级性能（`src/novel/cache.ts`：按新鲜度令牌复用的进程内扫描缓存）· 导出 / 快捷键 / 错误处理（`docs/13-p5-polish.md`，导出在真实实例上 HTTP 跑通）；面板交互待复核 |
| M7 | ✅ 代码完成、检查全绿；**真实实例 HTTP 全流程跑通**（记录/来源/行级 diff/回滚/四条错误路径），面板交互待复核 |
| 伏笔记录 | ✅ 代码完成、检查全绿；**真实实例跑通**建卡→记埋点→回收→检查页干净（`docs/12-threads.md`）；面板交互待复核 |

`pnpm run check` = typecheck + build + `format-check` **346 项** + `client-load-check` **21 次表面渲染** + 样式源码规则，**exit 0**。

### 验证到哪了

- **真实实例（HTTP）**：`/api/novel/search`（提问 / 关键词 / 空查询）与 `/api/novel/checks`（跑检查 / 忽略 / 保存报告 / 非法动作 400）都跑通了（`06` §3.3、`07` §3.3）。
- **真实 GUI**：任务能跑完并流式出正文、检索能跑通。
- **性能**：`spike/perf-check.mjs` 在真磁盘上量（1000 章 / 311 万字）：snapshot 1340→296 ms、检索 1453→340 ms、检查 1391→345 ms（4–5×，冷启动不变慢）；同一本书在**真实实例**上走 HTTP：1688→330–380 ms。**剩下的时间几乎全在 `dsh-fs-local` 列举时每个子项的 `realpath` 上**（`spike/fs-cost-check.mjs` 拆出来了），插件的能力面里躲不掉——详见 `10` §3.3、§5。**缓存的核心承诺**（面板外改一章，下一次请求就看得见；改回去立刻看不见）已在真实实例上跑通（`10` §4.2），走的是真 `ctx.fs` 与真令牌。
- **修改记录（M7）**：真实实例上跑通「手写保存 → 采纳「润色本章」后保存 → 列表 → 行级 diff → 回滚 → 读回」（`11` §4.2）。三件只有真跑才知道的事：任务来源确实随 HTTP 落到记录里；diff 里 `wordCount` 的变化如实报出；回滚把正文逐字换了回去。
- **伏笔记录**：真实实例上把「建卡 → 记埋点 → 回收 → 检查页干净 → 检索按生命周期作答」跑通了一遍（`12` §2.2）。两件只有真跑才知道的事：回收之后检查页确实干净（`status` 与 `payoffIn` 同一次写入，M6 读的是同一份记录）；面板拿到的摘要与卡片文件一致（原句没在某一层被丢掉）。
- **导出（P5）**：真实实例上跑通「ping 见新路由 → 全书 md / txt → 单卷 → 写进 `exports/` 并逐字读回 → 导出不留修改记录 → 不存在的章与缺 volume 各 400」（`13` §2.2）。三件只有真跑才知道的事：**章标题叠号**（建章表单存的就是「第二章 巡夜人」，导出成了 `第 2 章 第二章 巡夜人`，已修成「同号才摘」）；导出确实**一次一个新文件**（`exports/` 里三个文件，没有互相覆盖）；`.novel/history/exports` 根本不存在——导出不是文档。
- **导出预览的 `head` 与 M6 的 `thread-quote`**（补做的两条欠账，`13` §1.1、§1.4）：真实实例上验过「`head=40` 只回 40 字符、`bytes` 报全文 146、统计仍是整份」「**POST 带 `head` 落盘的仍是完整文件**」，以及「故意写坏的伏笔卡 `th-901` → 检查页 1 条警告 → 把原句改回去（**只差一个逗号**）→ 0 条」。后一条同时证明 host 的规则与面板的「跳回埋点」对「同一句话」的容忍度一致。
- **「不要了的伏笔还在烦我」（作者报的 bug，`07` §2.4–2.5）**：作者真书上做了前后对比。第一半（**删除 = 存档**）：一张 `archived: true` 的同名伏笔在 → 修复前 2 条 `alias-clash` 错误，修复后 0 条。第二半（**放弃 = `abandoned`**，他实际按的是这个按钮）：修复前 `checks` 2 条错误、检索「test 埋在哪」解析出**两个**同名实体且答案取自**放弃的那一个**；修复后 `checks` 0 条、检索只解析出活着的 `th-002`。第三半（作者说「改」之后的**章节**侧）：隔离实例上同一个章文件只改 `archived` 一个字段——存档时 0 条、去掉存档立刻 5 条关于它的报错。判断收在 `src/novel/cards.ts` 的 `isRetiredCard()` 与 `checks.ts` 里的 `live`，面板与 host 共用。
- **设定页的两处（作者报的）**：①`世界观`/`时间线` 共用一个表单，而且那个表单对页面**绑错了字段**（读写的都是 `name`，页面用的是 `title`）——所以标题框是空的、打字写进一个没人读的字段；现在按文档类型绑 `title`，页面不再显示卡片的字段（id/别名/身份/标签/存档），改为一句「这一页是什么、谁在读它」，并且**卡片表单补上了 `age` 与 `gender`**（`gender` 是新的可选字段，`03` §4.3；年龄输入非数字不会被写成 `NaN`）。②**卡片字段此前一个都不进 prompt**（`cardBlocks` 只发正文）——现在 `cardFacts()` 把「名字/别名/身份/年龄/性别/标签」合成一行放在卡片块开头（实测 prompt：`## chen-mo` / `名字: 陈默｜别名: 默哥｜身份: 主角｜年龄: 19｜性别: 男`）。③**时间线有了自己的编辑器**（`TimelineEditor.tsx` + 共用的 `novel/timeline.ts`，M6 的两条规则改用同一份解析）；真实实例上一张故意写坏的报出 `timeline-ref …c9999@8` 与 `timeline-order …6>7`。
- **「标签里打不出逗号」+「身份只能是一个」（作者报的）**：①列表输入框的 `value` 此前直接绑**解析后的列表**，打一个逗号就被回写抹掉（`"a,"` → `['a']` → 显示 `"a"`），所以两个标签永远打不出来；现在统一走 `client/ListField.tsx`——**输入期间保留原文，blur/回车才解析**（别名、标签、大纲页的出场角色/地点一起修，它们是同一个 bug）。②`role` 现在**容忍标量与列表**（`role: [主角, 前朝皇子]`），面板写回时一个为标量、多个为列表（老文件一个字节不动），摘要与 prompt 里用 `、` 连成一行。真实实例上验过：文件里写 `role: [主角, 前朝皇子]` → 面板拿到的摘要是 `主角、前朝皇子`。
- **「地点、物品的编辑界面和角色卡一模一样」（作者报的）**：设定页此前对**所有**卡只渲染一套表单，类型只影响两处（单文件页走另一套、伏笔卡多一个生命周期下拉），于是地点卡也有身份/年龄/性别框，正文框的占位提示对每张卡都是角色的那五行。格式说地点/物品/势力只是**骨架同构**（`03` §4.3：`id`/`type`/`name`/`aliases`/`tags`/`archived` 相同），`age`/`gender` 是**角色的**展示事实，`role` 归角色（身份）与势力（立场）。现在「哪种卡有哪些字段 + 正文分节提示」是 `src/novel/cards.ts` 的 `CARD_FIELDS` / `CARD_BODY_HINT`：表单按它决定显示哪些框，`cardFacts()` 按它决定发哪些字段——手写进地点卡的 `age: 三百年` 仍留在文件与摘要里（文件里的事实不在面板里消失），但不会再以「年龄」进 prompt。检查新增 4 条（当时总计 335）。测试书里已有的 `settings/locations/qingshi-town.md`（只有 `id`/`type`/`name` + 地理/氛围）与 `novel/settings/locations/tian-yan-zong.md`（地理/氛围/势力归属/硬约束）本来就是按类型写的。
- **「不同流派的境界设定放哪」（作者问的）→ 通用设定卡 + 章节 `refs`**：查链路时先纠正了一个误解——`settings/world.md` **不是**每次生成都读它，`client/tasks.ts` 里它只被「一致性检查（模型）」**整份**读入（`include(ctx, WORLD_FILE, …)`），写正文读的是文风规则、风格样本、**本卷卷纲**、本章要点与本章 frontmatter 引用的卡。所以「全堆世界观」的真实代价是每次模型检查变胖 + 一个文件里总纲与细则混着。现在新增卡类型 **`settings/lore/<id>.md`（`type: lore`，面板标签「设定」）**：一个流派一张卡，正文分节「定义/分级/条件与代价/边界」，阶梯写「分级」；卡类型本来就有扩展口（`paths.ts` 的 `CARD_DIRS` + 一个 `type` 值），加完自动获得分组列表、检索实体问答、`missing-ref` 等规则覆盖。章节 side 新增 frontmatter 字段 **`refs`**（与 `characters`/`locations` 并列，`03` §3.2 引用表加一行），章纲编辑器多一个「引用的设定 id」框，拆章任务的输出形状里也多了 `refs`；被引用的卡与出场角色卡走同一条路进**生成** prompt（「按章纲写整章」），所以「开章时引用一下流派设定」直接生效。面板里世界观那一页的提示语此前写着「任务装配会读硬约束那一节」，那是假的，一并改成事实。检查新增 11 条（总计 346）。
- **我没能验证的两件事**，都只能由作者点（沙箱够不到）：
  1. **模型的那两轮**（M6 模型检查、M4 去 AI 味检查 / 润色）——隔离实例没有可用的模型路由，会话以 `reason: 'error'` 结束、日志里只有 header。
  2. **面板交互**：九份文档 §4/§2.3 各有一张清单（`05` 17 项 / `06` 10 项 / `07` 16 项 / `08` 11 项 / `09` 8 项 / `10` 8 项 / `11` 15 项 / `12` 12 项 / `13` 15 项）。**优先点这九件事**：检索页三种问法（`06`）→ 检查页确定性报告 + 忽略记忆（`07`）→ 模型检查一轮（`08`）→ 去 AI 味检查 + **定位到正文**（`09`）→ **导出页的预览 / 导出 / 下载 + 键盘快捷键**（`13`）→ **检查页那条「原句找不到了」+ 伏笔页签跳回埋点**（`07` 第 13/14 项）→ **删掉一条伏笔看它是否真的消失**（`07` 第 15 项）→ **存档一章看检查里是否不再念它**（`07` 第 16 项）→ **设定页：世界观/时间线各自的样子 + 时间线逐行编辑 + 角色卡的年龄与性别**（测试书里留了一张故意写坏的时间线，`settings/timeline.md`；另外点一张**地点卡**，它不该再有年龄/性别/身份框，正文框的提示该是「地理 / 氛围 / 势力归属」）→ **在一张卡的标签里打「灵异,悬疑」看逗号是否留住**，以及**身份框里写两个身份**（测试书里留了 `settings/characters/probe-role.md`，它的 `role` 本来就是两个）。加一件 M7 的：**先点出一张角色卡再点「修改记录」**（`11` §4.1b）。再加一件这一轮的：**在设定页新建一张「设定」卡（例如 id 写 `jian-xiu-jingjie`，正文「## 分级」里写你的境界阶梯），然后在某一章的章纲里把它填进「引用的设定 id」，点「按章纲写整章」**——看输入清单里有没有那张卡、正文有没有照着阶梯写；再故意把 `refs` 写成一个不存在的 id，看检查页有没有报 `missing-ref`。
- **本轮修掉的真 bug**（都来自真跑，不是读代码）：模型调用失败时面板把空结果当「生成完成」（`08` §2.3）；风格样本从来没进过 prompt（`09` §2.1）；P5 的缓存把「没有新鲜度令牌」当成了「不读这个文件」（`10` §4.1）；M7 的同一毫秒两版 `at` 相同导致**回滚回到错误的版本**、以及回滚被记成普通的「修改」（`11` §4.1，两条都是被自己的断言当场抓住的）；**M7 交付后作者点出「修改记录只看得到章节，看不到卡」**（`11` §4.1b）；抽取定位逻辑时抓出的 P4 老 bug——兜底只忽略空白却写着标点、选区结尾按引文长度相加导致短一截（`11` §4.1c）；**导出把自带章号的标题叠成「第 2 章 第二章 巡夜人」**（`13` §1.1，读真实输出才看见）；以及**作者连着报了两次、最后说「改」的那一类：「不要了的文件还在对我说话」**（`07` §2.4–2.5：删掉的卡、放弃的线、存档的章，此前都还在各自的规则里出现；根因是四处各自判断「还算不算数」，其中三处只看 `archived`）。

### 四条会绊住下一次的环境事实
1. **host 半要换进程**：`pnpm run build` 之后浏览器半刷新即生效，**host 半（新路由/新行为）必须有一个新进程**——`patchReload: live` 只热重载 `cordis.patch.yml`，不替换插件模块（源码证据与判定方法见 `01` §4）。新路由报 404 时先看 `/api/novel/ping` 返回的 `routes`。**导出这轮又踩了一次**：第一次探针跑在旧进程上，`/api/novel/export` 还不存在。
2. **隔离实例**：`DSH_HOME=.dsh-home`、`DSH_NOVEL_ROOT=.spike-novel`（已 gitignore），里面是我留的**故意写坏的测试书**。它的形状（下一段对话接手时别被它骗了）：正文「御剑而起」vs 卡片「尚未筑基」（模型检查用）；`chapters/v01/c0900.md`（**已存档**，pov/location 指向不存在的卡、字数严重不足）；`settings/threads/th-800.md`（已存档）与 `th-801.md`（同名活卡）；`th-900.md`（原句只差一个逗号）、`th-901.md`（原句已失效）；`settings/timeline.md`（**故意倒序 + 引用不存在的章**）；`settings/characters/probe-role.md`（两个身份、两个标签）。启动命令见上文「开发」。另外我的沙箱里覆盖写已存在的文件会被 Windows DACL 拦（`06` §3.3），新建文件正常——**M7 这轮又踩了一次**：第一次跑修改记录的探针时两次写入其实全部 403，列表里只剩脚手架那一版，看起来像「M7 没记录」；把实例进程换成不受文件围栏约束的启动方式后才跑通 update 与回滚（`11` §4.2）。**在隔离实例上验任何「改写已有文件」的路径之前，先确认这一步没在被静默拦掉。** P5 另加一个 `.spike-novel-perf/`（同样已 gitignore）：`spike/perf-check.mjs` 每次运行重新生成的合成百万字工程，几 MB 生成文本，不是产品的一部分。
3. **别用 PowerShell 重写源码文件**：这轮用 `Get-Content -Raw` + `Set-Content` 批量改 `src/client/Panel.tsx`，本机把它按 ANSI 码页解码再写回，中文全坏、文件不再合法 UTF‑8（`git checkout --` 才救回来）。要用 `edit`/`write` 工具改文件；非要用 PowerShell，读写两侧都显式 `-Encoding utf8`。**同一类坑还有两个，都是这轮踩的**：`Set-Content -Encoding utf8` 会**加 BOM**，而 BOM 让插件的 frontmatter 解析整个失效（首行 `---` 匹配不上 → 字段全空、正文里混进 frontmatter）；`.cmd` 里的 `chcp 65001` 会让后面 cmd 自己的 `set /p` **读不到重定向输入**（实测：带 chcp 时第一个提示就拿到空值，不带 chcp 时两个提示都正常）——所以批处理只负责切代码页 + 调 node + `pause`，提示一律写在 Node 里。
4. **`.mjs` 不能双击运行**：没关联时双击只是「选择打开方式」，关联到 `node.exe` 时双击**没有参数、窗口一闪就关**。要双击就用 `.cmd` 包装（`tools/*.cmd`），并且**交互提示放在 Node 脚本里**（见上一条的 `chcp` 坑）。

### 下一步（按优先级）

1. **接着欠账清单往下走**：`13` §4 里剩下的都不大（重试的 `redo` 只覆盖三条路径、下载走 Blob 而浏览器不会告诉你成功与否、面板其他位置的章标题仍会与自带章号叠加）；`12` §4 里最值钱的是**「按章看伏笔」视图**（现在只能靠检索或自己扫列表）。**性能那块的下一步不是继续优化插件**——暖扫描剩下的大头（400 章约 120 ms、1000 章约 300 ms）几乎全是 `dsh-fs-local` 列举时每个子项的 `realpath`，插件的能力面里没有更便宜的列举原语，目录令牌又不能用来跳过列举（`10` §3.4 有速率证据）。真要再快，得走 DSH 侧：轻量列举，或文件变更通知（需求 §6 提过 `workspaceFiles.changes`，**可用性未验证**）。**先量一次再动手。**
2. **小而完整**：M5 的模型问答（用检索出的片段回答开放问题）；把「润色 / 去 AI 味」接到**选中段落**（需要编辑器支持范围选择）。
3. **M7 的后续**（都在 `11` §5）：面板外的改动不进记录（所以「最新一版」可能与磁盘不一致）；diff 没有左右对照视图；历史列表只取最近 50 条、没有翻页。（「记录写入失败时面板不会说」已在 `13` 结清：现在随写入结果带回警告。）
4. **伏笔的后续**（都在 `12` §4）：~~原句会过期而没有检查规则~~ → **已做**（`13` §1.4：`findQuote` 移到 `src/novel/quote.ts` 两边共用，M6 新增 `thread-quote`；它**只报不改**，不会替作者把原句换成现在那一句）；剩下的是「按章看伏笔」的视图、`plannedPayoff` 与 `reinforcedIn` 只能手改。
5. **两件问过、作者还没答的小事**（都问在对话里，写在这里免得丢）：
   - **解析器要不要容忍开头 BOM**：PowerShell 的 `Set-Content -Encoding utf8` 会写 BOM，而 BOM 让 `parseDocument` 的 `---` 匹配不上（字段全空、frontmatter 混进正文）。作者现在的书里 0 个 BOM 文件，所以只记录没改；改法是一行（读入时去掉开头的 `\uFEFF`）+ 一条断言。
   - **「移动」一个 id（改卡的 id/类型、改章节文件名）**：面板做不到（`ctx.fs` 没有 move/rename），三条路等作者选——**A** 扫出所有指向旧 id 的引用并一起改（写新文件 + 存档旧的）、**B** 只留成死档并让检查页报出断掉的引用（小得多）、**C** 现状（在文件管理器里改，检查页报出哪里断了）。

### 明确的欠账

物理删除（DSH 没有删除原语，插件不越沙箱）；未保存缓冲只在切章节/切卡时提示（关页面也提示了，切页签不提示——切页签不丢缓冲，所以只是没提醒）；`beats` 只有 ↑/↓ 没有拖拽；模型层不支持「忽略某条」（`08` §2.4，理由与备选都在那里）；段落级润色未做（`09` §5）；模型的两轮与页面交互未验证（见上）；**暖扫描仍是 O(文件数)，瓶颈在 `dsh-fs-local` 的逐子项 `realpath`（1000 章约 300 ms），插件内无解**（`10` §5）；扫描缓存没有上限或淘汰，持仓随书增长（一本百万字的书约十几 MB）；**修改记录只记面板的写入**，在 VSCode 里直接改文件不会产生版本（`11` §5）；**伏笔的原句会过期**——现在检查页会报（`thread-quote`），但它不会替作者改，也没有「按章看伏笔」的视图（`12` §4）；**导出预览只取开头、渲染仍是整本**，下载成功与否浏览器不说，快捷键不可自定义，面板其他位置的章标题仍会与自带章号的标题叠加（`13` §4）；**检索的答案句会点出存档章**（`07` §2.5 已让它明说，但「优先报活章、只在没有活章时才提存档章」还没做）。
